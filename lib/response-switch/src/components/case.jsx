// @todo: validate the match function as best as possible.
function Case({ path, status, json, children }) {
  // @todo: validate that children is a single DOM node, not an array of nodes.
  return children;
}

export default Case;
